enum UserRole {
  user
  admin
  merchant
}

enum UserType {
  customer
  seller
}

enum UserStatus {
  active
  deleted
  restricted
  pending
  banned
}

enum MerchantType {
  individual
  branch
}

enum OtpType {
  PHONE
  EMAIL
  RESET_PASSWORD
  RESET_PIN
}

enum OnboardingStep {
  PHONE_VALIDATED // Step 1: Phone checked (and verified via OTP if applicable)
  PROFILE_COMPLETED // Step 2: Basic info submitted
  BUSINESS_PROFILE_COMPLETED // Step 3: Seller details  submitted
  LIVENESS_PASSED // Step 3: Face scan successfully verified by AWS Rekognition
  PIN_COMPLETED // Step 4: Security PIN is set. User is fully onboarded.
}

enum LedgerType {
  CREDIT
  DEBIT
}

enum TransactionType {
  TRANSFER_MYCHANGE
  TRANSFER_BANK
  GIVE_CHANGE
  SETTLEMENT_SWEEP
  DEPOSIT
}

enum ChargeType {
  FIXED
  PERCENTAGE
}

enum WalletType {
  USER
  SYSTEM_FEE
  TRANSIT
  SETTLEMENT
}

enum TransactionStatus {
  PENDING
  SUCCESS
  FAILED
  PARTIAL_SUCCESS
  REVERSED
}

enum WebhookStatus {
  PENDING
  SUCCESS
  FAILED
  IGNORED
}
